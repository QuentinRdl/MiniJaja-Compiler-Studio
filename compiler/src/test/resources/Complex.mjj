class C {
  int x;
  int y = 10;
  final int VAL = 5;
  boolean b1 = true;
  boolean b2 = false;

   main {
 	if (b1 && (b1 || (!b2))) {
		x = VAL+y-0;
	} else {
		x++;
	};
	x = y * z;
	x = x / z;
	x++;
   }
}