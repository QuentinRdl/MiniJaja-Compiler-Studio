package fr.ufrst.m1info.gl.compgl.driver;


import javafx.stage.Stage;
import org.junit.jupiter.api.Test;
import org.testfx.framework.junit5.ApplicationTest;

import static org.testfx.api.FxAssert.verifyThat;
import static org.testfx.matcher.control.LabeledMatchers.hasText;

public class Example_TestFX_Test extends ApplicationTest {

    /*

    @Override
    public void start(Stage stage) throws Exception {
    }

    @Test
    void should_click_button_and_change_label_text() {
        // Check original text
        verifyThat(".label", hasText("Hello TestFX!"));

        // Action that triggers the click on the button
        clickOn("Click me");

        // Check that the text is updated
        verifyThat(".label", hasText("Button clicked!"));
    }

    */
}